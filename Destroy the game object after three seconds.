using UnityEngine;

public class AfterThreeSecondsDestroyObject : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(gameObject,3f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
