using UnityEngine;

public class ArraysLesson : MonoBehaviour {
    public int a = 1;
    public int[] arr = {0, 1, 2, 3, 4, 5};
    // Use this for initialization
    void Start () {
        Debug.Log(arr[3]);
    }
    
    // Update is called once per frame
    void Update () {
     
    }
}
